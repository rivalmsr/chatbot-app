{
  "name": "build-ai-powered-apps",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "bun run index.ts",
    "format": "prettier --write .",
    "prepare": "husky",
    "preinstall": "bun --version || (echo '❌ Please use Bun' && exit 1)"
  },
  "devDependencies": {
    "@tailwindcss/typography": "^0.5.19",
    "@types/bun": "latest",
    "concurrently": "^9.2.1",
    "husky": "^9.1.7",
    "lint-staged": "^16.2.7",
    "prettier": "^3.8.0"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "workspaces": [
    "packages/*"
  ],
  "engines": {
    "bun": ">=1.1.0",
    "node": "please use bun",
    "npm": "please use bun",
    "yarn": "please use bun",
    "pnpm": "please use bun"
  }
}
